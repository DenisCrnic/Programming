def createJavaTable(a, name):
    x = "String[] " + str(name) + " = {"
    for i in range(0,len(a)-1):
        if(i == (len(a)-1):
            x += i
        else:
            x += i + ','
    x += '};'
    print x

x = ["denis", "tadej"]
createJavaTable(x, "java.exe")