def createJavaTable(a, name):
    x = "String[] " + str(name) + " = {"
    for i in range(0,len(a)):
        if(i == (len(a)-1)):
            x += a[i]
        else:
            x += a[i] + ', '
    x += '};'
    print x

x = ["denis", "tadej"]
createPythonTable(x, "java.exe")